-r requirements.txt

# Optional deterministic model export/runtime support.
# TensorRT itself is platform/CUDA-specific and must be installed separately.
onnx>=1.12,<2
onnxslim>=0.1.96,<1
openvino>=2024,<2027
